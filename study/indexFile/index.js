
console.log('roading file : index.js');

/*

const cal = {
    plus : function(a,b) {return a+b;}
}

console.log(cal.plus(1,2));

const chk = prompt("text test");
    console.log(chk);
    if(chk === "Y"){
        const title = document.getElementById("title");
        console.log(title);
        let title2 = document.querySelector('#title');
        console.log(title2);
        const headtag = document.getElementsByTagName("h1");
        headtag[0].innerHTML ="change title";
        title.innerText ="change2 title";
        document.title = "document title change";
        console.log(document);
        console.log(document.lastElementChild.innerHTML);
        function resizeHandle(event){
            console.log(event);
            console.log("window resized");
        }
        window.addEventListener("resize",resizeHandle);
        function handleClick(event){
            title.style.color = "red";
        }
        title.addEventListener("click",handleClick);
    }

    const title = document.querySelector("#title");
    const BASE_COLOR = "white";
    const OTHER_COLOR = "red";

    function handleClick() {
            const currectColor = title.style.color;
            if(currectColor === BASE_COLOR){
                title.style.color = OTHER_COLOR;
            }else{
                title.style.color = BASE_COLOR;
            }
        }

    function init() {
        title.style.color = BASE_COLOR;
        title.addEventListener("click",handleClick);
        title.addEventListener("mouseenter",handleClick);
    }
    init();
 */